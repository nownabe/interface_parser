require 'spec_helper'

describe InterfaceParser do
  it 'has a version number' do
    expect(InterfaceParser::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
